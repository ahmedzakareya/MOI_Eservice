@model RequestBaseVM

@{
    ViewBag.Title = "WhoConcTourLicRequest";
    Layout = "~/Views/Shared/_LayoutEservice.cshtml";
    var token = Context.Session.GetString("UserToken");
    var userId = Context.Session.GetString("UserId");
    var userName = Context.Session.GetString("UserUserName");
    var civilId = Context.Session.GetString("UserCivilId");
    var fullName = Context.Session.GetString("UserFullName");
    var accountTypeId = Context.Session.GetString("UserAccouuntTypeId");
}




<div class="my-profile-box">
    <h3>طلب شهادة لمن يهمه الامر لترخيص تشغيلي رقم : @Model.LicencesVM.LicNo / @Model.LicencesVM.ActivityTypeName</h3>
    @*@using (Html.BeginForm(new { enctype = "multipart/form-data" }))*@
    <form id="RequestForm" enctype="multipart/form-data">
        @Html.AntiForgeryToken()
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })

<div class="row align-items-center">

            <div class="col-xl-4 col-md-6 col-sm-12">
                <div class="form-group">
                    <label asp-for="@Model.LicencesVM.LicNo"></label>
                    <input asp-for="@Model.LicencesVM.LicNo" class="form-control" placeholder="License No" readonly />
                    <span asp-validation-for="@Model.LicencesVM.LicNo" class="text-danger"></span>
                </div>
            </div>
            <input type="hidden" asp-for="LicencesVM.LicId" />
            <input type="hidden" asp-for="LicencesVM.ActivityTypesLookup.ActivityCode" />
            <input type="hidden" asp-for="LicencesVM.ApplicantId" />
            <input type="hidden" asp-for="LicencesVM.BuildingId" />
            <input type="hidden" asp-for="LicencesVM.CompanyId" />
            <input type="hidden" asp-for="LicencesVM.ApplicantCivilId" />
            <input type="hidden" asp-for="LicencesVM.LicNo" />
            <input type="hidden" asp-for="LicencesVM.PreApprovalId" />
            <input type="hidden" asp-for="LicencesVM.ActiivityTypeId" />
            <input type="hidden" asp-for="LicencesVM.Licowner" />
            <input type="hidden" asp-for="LicencesVM.LicName" />
            <input type="hidden" asp-for="LicencesVM.Company.CommercialLicNo" />
            <input type="hidden" asp-for="LicencesVM.Company.DirCompanyAr" />
            <input type="hidden" asp-for="LicencesVM.Company.OwnerCompanyAr" />
            <input type="hidden" asp-for="LicencesVM.ManagerCivilId" />
            <input type="hidden" asp-for="LicencesVM.CommercialLicNo" />



            <input type="hidden" asp-for="ReqtypeId" />
            <div class="col-xl-4 col-md-6 col-sm-12">
                <div class="form-group">
                    <label asp-for="@Model.LicencesVM.IssueDate"></label>
                    <input asp-for="@Model.LicencesVM.IssueDate" class="form-control" placeholder="License Issue Date" readonly />
                    <span asp-validation-for="@Model.LicencesVM.IssueDate" class="text-danger"></span>
                </div>
            </div>

            <div class="col-xl-4 col-md-6 col-sm-12">
                <div class="form-group">
                    <label asp-for="@Model.LicencesVM.ExpireDate"></label>
                    <input asp-for="@Model.LicencesVM.ExpireDate" class="form-control txtDate" placeholder="License Expiry Date" readonly />
                    <span asp-validation-for="@Model.LicencesVM.ExpireDate" class="text-danger"></span>
                </div>
            </div>

            <div class="col-xl-4 col-md-6 col-sm-12">
                <div class="form-group">
                    <label asp-for="@Model.LicencesVM.ApplicantCivilId"></label>
                    <input asp-for="@Model.LicencesVM.ApplicantCivilId" class="form-control" placeholder="User Civil ID" readonly />
                    <span asp-validation-for="@Model.LicencesVM.ApplicantCivilId" class="text-danger"></span>
                </div>
            </div>

            <div class="col-xl-4 col-md-6 col-sm-12">
                <div class="form-group">
                    <label asp-for="@Model.AspnetUserVM.FullNameAr"></label>
                    <input asp-for="@Model.AspnetUserVM.FullNameAr" class="form-control" placeholder="User Name" readonly />
                    <span asp-validation-for="@Model.AspnetUserVM.FullNameAr" class="text-danger"></span>
                </div>
            </div>

            <div class="col-xl-4 col-md-6 col-sm-12 d-none">
                <div class="form-group">
                    <label asp-for="@Model.LicencesVM.CompanyId"></label>
                    <input asp-for="@Model.LicencesVM.CompanyId" class="form-control" placeholder="Company ID" readonly />
                    <span asp-validation-for="@Model.LicencesVM.CompanyId" class="text-danger"></span>
                </div>
            </div>

            <div class="col-xl-4 col-md-6 col-sm-12">
                <div class="form-group">
                    <label asp-for="@Model.LicencesVM.LicName"></label>
                    <input asp-for="@Model.LicencesVM.LicName" class="form-control" placeholder="Hotel Name" readonly />
                    <span asp-validation-for="@Model.LicencesVM.LicName" class="text-danger"></span>
                </div>
            </div>

            <div class="col-xl-4 col-md-6 col-sm-12">
                <div class="form-group">
                    <label asp-for="@Model.AspnetUserVM.FullNameAr"></label>
                    <input asp-for="@Model.AspnetUserVM.FullNameAr" class="form-control" placeholder="Owner Company (Arabic)" readonly />
                    <span asp-validation-for="@Model.AspnetUserVM.FullNameAr" class="text-danger"></span>
                </div>
            </div>

            <div class="col-xl-4 col-md-6 col-sm-12 d-none">
                <div class="form-group">
                    <label asp-for="@Model.LicencesVM.CommercialLicNo"></label>
                    <input asp-for="@Model.LicencesVM.CommercialLicNo" class="form-control" placeholder="Owner Company Commercial No" readonly />
                    <span asp-validation-for="@Model.LicencesVM.CommercialLicNo" class="text-danger"></span>
                </div>
            </div>

            @* <div class="col-xl-4 col-md-6 col-sm-12 d-none">
                <div class="form-group">
                    <label asp-for="@Model."></label>
                    <input asp-for="OwnerCoAddress" class="form-control" placeholder="Owner Company Address" readonly />
                    <span asp-validation-for="OwnerCoAddress" class="text-danger"></span>
                </div>
            </div> *@

            <div class="col-xl-4 col-md-6 col-sm-12 d-none">
                <div class="form-group">
                    <label asp-for="@Model.LicencesVM.ManagerId"></label>
                    <input asp-for="@Model.LicencesVM.ManagerId" class="form-control" placeholder="Licence ID" readonly />
                    <span asp-validation-for="@Model.LicencesVM.ManagerId" class="text-danger"></span>
                </div>
            </div> <div class="col-xl-4 col-md-6 col-sm-12 d-none">
                <div class="form-group">
                    <label asp-for="@Model.LicencesVM.CompanyId"></label>
                    <input asp-for="@Model.LicencesVM.CompanyId" class="form-control" placeholder="Licence ID" readonly />
                    <span asp-validation-for="@Model.LicencesVM.CompanyId" class="text-danger"></span>
                </div>
            </div>
            <div class="col-xl-4 col-md-6 col-sm-12 d-none">
                <div class="form-group">
                    <label asp-for="@Model.LicencesVM.ActiivityTypeId"></label>
                    <input asp-for="@Model.LicencesVM.ActiivityTypeId" class="form-control" placeholder="Licence ID" readonly />
                    <span asp-validation-for="@Model.LicencesVM.ActiivityTypeId" class="text-danger"></span>
                </div>
            </div>

            <div class="col-xl-4 col-md-6 col-sm-12 d-none">
                <div class="form-group">
                    <label asp-for="@Model.LicencesVM.BuildingId"></label>
                    <input asp-for="@Model.LicencesVM.BuildingId" class="form-control" placeholder="Licence ID" readonly />
                    <span asp-validation-for="@Model.LicencesVM.BuildingId" class="text-danger"></span>
                </div>
            </div>

            <div class="col-xl-4 col-md-6 col-sm-12 d-none">
                <div class="form-group">
                    <label asp-for="@Model.LicencesVM.ClassificationId"></label>
                    <input asp-for="@Model.LicencesVM.ClassificationId" class="form-control" placeholder="Licence ID" readonly />
                    <span asp-validation-for="@Model.LicencesVM.ClassificationId" class="text-danger"></span>
                </div>
            </div>
            <div class="col-xl-4 col-md-6 col-sm-12 d-none">
                <div class="form-group">
                    <label asp-for="@Model.LicencesVM.LicStatusId"></label>
                    <input asp-for="@Model.LicencesVM.LicStatusId" class="form-control" placeholder="Licence ID" readonly />
                    <span asp-validation-for="@Model.LicencesVM.LicStatusId" class="text-danger"></span>
                </div>
            </div>

            <div class="col-xl-4 col-md-6 col-sm-12 d-none">
                <div class="form-group">
                    <label asp-for="@Model.LicencesVM.RecordNo"></label>
                    <input asp-for="@Model.LicencesVM.RecordNo" class="form-control" placeholder="Licence ID" readonly />
                    <span asp-validation-for="@Model.LicencesVM.RecordNo" class="text-danger"></span>
                </div>
            </div>
            <div class="col-xl-4 col-md-6 col-sm-12 d-none">
                <div class="form-group">
                    <label asp-for="@Model.LicencesVM.PreApprovalNo"></label>
                    <input asp-for="@Model.LicencesVM.PreApprovalNo" class="form-control" placeholder="Licence ID" readonly />
                    <span asp-validation-for="@Model.LicencesVM.PreApprovalNo" class="text-danger"></span>
                </div>
            </div>
            <div class="col-xl-4 col-md-6 col-sm-12 d-none">
                <div class="form-group">
                    <label asp-for="@Model.LicencesVM.PreApprovalId"></label>
                    <input asp-for="@Model.LicencesVM.PreApprovalId" class="form-control" placeholder="Licence ID" readonly />
                    <span asp-validation-for="@Model.LicencesVM.PreApprovalId" class="text-danger"></span>
                </div>
            </div>
            <div class="col-xl-4 col-md-6 col-sm-12 d-none">
                <div class="form-group">
                    <label asp-for="@Model.LicencesVM.LicId"></label>
                    <input asp-for="@Model.LicencesVM.LicId" class="form-control" placeholder="Licence ID" readonly />
                    <span asp-validation-for="@Model.LicencesVM.LicId" class="text-danger"></span>
                </div>
            </div>

            <input type="hidden" asp-for="@Model.LicencesVM.ManagerId" />
            <input type="hidden" asp-for="@Model.LicencesVM.ApplicantId" />
            <input type="hidden" asp-for="@Model.LicencesVM.RecordNo" />
            <input type="hidden" asp-for="@Model.LicencesVM.CommercialLicNo" />
            <input type="hidden" asp-for="@Model.LicencesVM.ManagerCivilId" />
            <input type="hidden" asp-for="@Model.LicencesVM.MarketingManagerCivilId" />
            <input type="hidden" asp-for="@Model.LicencesVM.OperationsManagerCivilId" />
            <input type="hidden" asp-for="@Model.LicencesVM.SalesManagerCivilId" />
            <input type="hidden" asp-for="@Model.LicencesVM.SalesManagerId" />
            <input type="hidden" asp-for="@Model.LicencesVM.MarketingManagerId" />
            <input type="hidden" asp-for="@Model.LicencesVM.OperationsManagerId" />

            <div class="col-xl-4 col-md-6 col-sm-12">
                <div class="form-group">
                    <label asp-for="@Model.LicencesInfo.FixedFees"></label>
                    <input asp-for="@Model.LicencesInfo.FixedFees" class="form-control" placeholder="Service Amount" required readonly />
                    <span asp-validation-for="@Model.LicencesInfo.FixedFees" class="text-danger"></span>
                </div>
            </div>

            <div class="col-xl-12 col-lg-12 col-md-12">
        <div class="form-group">
            <h3>المرفقات</h3>
        </div>
        <div class="form-group">
            <h6 class="text-danger" style="padding-right: 10px;">## حجم الملف يجب ان لا يتجاوز 2 ميجا</h6>
        </div>
    </div>

            @foreach (var config in Model.FileUploadConfigs)
            {
                //var fieldName = $"ContractDocuments[{config.FieldName}]";

                <div class="col-xl-4 col-md-6 col-sm-12">
                    <div class="form-group">
                        <label for="@config.FieldName" class="@((config.IsMandatory) ? "Required" : "")">
                            @config.AttachName
                            @if (config.IsMandatory)
                            {
                                <span style="color: red;">*</span> <!-- Only add one red star if required -->
                            }
                        </label>



                        <input type="file" name="NamedFile[0].File" class="form-control custom-file-input" required="@config.IsMandatory" accept="@config.AllowedFileTypes" />
                        <input type="hidden" name="NamedFile[0].FieldName" value="@config.FieldName" />
                        <input type="hidden" class="field-label" value="@config.AttachName" /> <!-- 👈 add this -->


                    </div>
                </div>
            }


            <script src="~/Content/assets/js/jquery-1.4.1.min.js"></script>

    <script type="text/javascript">
        jQuery(document).ready(function () {
            //alert("tetet");

            //get_sbu();

            loadImage();
            Swal.fire({
                icon: 'warning',
                title: 'تنويه ،،،!',
                text: 'يرجى التأكد من صحة بيانات المنشأة بالكامل وتحديث المرفقات في حال انتهاء صلاحيتها.',
            });
        });
        $(document).ready(function () {
            $('form').submit(function () {
                // Disable the submit button
                $(':submit', this).prop('disabled', true);
                $(this).append('<div class="preloader-area"> ' +
                    '<div class="spinner">' +
                    '   <div class="inner">' +
                    '      <div class="disc"></div>' +
                    '   <div class="disc"></div>' +
                    '     <div class="disc"></div>' +
                    '  </div>' +
                    ' </div>' +
                    '</div>');
                // Optionally, you can show a loading spinner or perform other actions
                $('.preloader-area').fadeOut(15000);
                // Continue with form submission
                return true;
            });
        });
        //function get_sbu() {
        //    var id = $('#ActivitiList').find(":selected").attr('value');
        //    $.ajax({
        //        type: "POST",
        //        url: "/Requests/GetActivity",
        //        data: "id=" + id,
        //        success: function (data) {
        //            if (id != "0") {
        //                if (data.length > 0) {
        //                    $('#ActivitiID').val(data[0].Value);
        //                    $('#ActivitiName').val(data[0].Text);
        //                } else {
        //                    $('#ActivitiID').val('');
        //                    $('#ActivitiName').val('');
        //                }
        //            }

        //        }
        //    });
        //}


        //---------- sweet alert  ------------

    $(function loadImage() {

                var errorMsg = '@ViewBag.test'
                var msg = '@ViewBag.Message'
                var id = '@ViewBag.id'


                if (msg == 'Success') {
                    Swal.fire({
                        icon: 'success',
                        title: 'تأكـيد',
                        text: 'تم حفظ البيانات بنجاح',
                        confirmButtonColor: '#7FB3D5',
                        confirmButtonText: 'الانتقال لقائمة الطلبات !'
                    }).then(function () { window.location = '../../TourismHome/RequestsList'; });

                }
                else if (msg == 'error') {
                    Swal.fire({
                        icon: 'error',
                        title: 'تأكـيد',
                        text: errorMsg,
                        confirmButtonColor: '#7FB3D5'
                        //confirmButtonText: 'الانتقال لقائمة الطلبات !'
                    }).then(function () { window.location = '../../TourismHome/LicenseDetails/' + id; });
                };


            });

    </script>


    <div class="col-xl-12 col-lg-12 col-md-12">
        <div class="form-group">
            <h3></h3>
        </div>
    </div>

    <div class="col-lg-12 col-md-12">
                <button type="button" onclick="submitFormViaAjax()" class="default-btn"> إرسال الطلب  <i class="flaticon-plus"></i></button>
    </div>
</div>
    </form>
</div>
@section Scripts {


<script>
        jQuery(document).ready(function () {
            Swal.fire({
                icon: 'warning',
                title: 'تنويه ،،،!',
                text: 'يرجى التأكد من صحة بيانات المنشأة بالكامل وتحديث المرفقات في حال انتهاء صلاحيتها.',
            });
        });

        $(document).ready(function () {
            $('form').submit(function () {
                // Disable the submit button
                $(':submit', this).prop('disabled', true);
                $(this).append('<div class="preloader-area"> ' +
                    '<div class="spinner">' +
                    '   <div class="inner">' +
                    '      <div class="disc"></div>' +
                    '   <div class="disc"></div>' +
                    '     <div class="disc"></div>' +
                    '  </div>' +
                    ' </div>' +
                    '</div>');
                // Optionally, you can show a loading spinner or perform other actions
                $('.preloader-area').fadeOut(15000);
                // Continue with form submission
                return true;
            });
        });
    async function submitFormViaAjax() {
        const uploadedFiles = new FormData();

        // 🟢 1. Add all normal text inputs (non-file)
        $('#RequestForm').serializeArray().forEach(function (input) {
            uploadedFiles.append(input.name, input.value);
        });
        var ActivityTypeId = $('#ActivitiList').val();  // Get selected value from the dropdown
        uploadedFiles.append('ActivityTypeId', ActivityTypeId);
        console.log('Selected Activity Value:', ActivityTypeId);
        var ReasonId = $('#EndReasonId').val();
        console.log('Selected Activity Value:', ReasonId);

        uploadedFiles.append("EndReasonId", $('#EndReasonId').val());

        let fileIndex = 0;

        // 🟢 2. Add file inputs with dynamic names like ContractDocuments[FieldName]
        const dynamicFileInputs = document.querySelectorAll('#RequestForm input[type="file"]');

        dynamicFileInputs.forEach((input) => {
            const files = input.files;

            const formGroup = input.closest('.form-group');
            const hiddenInput = formGroup.querySelector('input[type="hidden"][name$=".FieldName"]');
            const labelInput = formGroup.querySelector('input.field-label');

            const fieldName = hiddenInput ? hiddenInput.value : `UnknownField_${fileIndex}`;
            const labelName = labelInput ? labelInput.value : '';
            const isRequired = input.hasAttribute('required');

            if (files.length > 0) {
                for (let i = 0; i < files.length; i++) {
                    uploadedFiles.append(`NamedFile[${fileIndex}].FieldName`, fieldName);
                    uploadedFiles.append(`NamedFile[${fileIndex}].LabelName`, labelName); // ✅ here
                    uploadedFiles.append(`NamedFile[${fileIndex}].File`, files[i]);
                    uploadedFiles.append(`NamedFile[${fileIndex}].IsRequired`, isRequired ? "true" : "false");
                    fileIndex++;
                }
            } else {
                console.warn(`⚠️ No file selected for field: ${fieldName}`);
            }
        });

        // 🟡 Optional: Debug output
        for (let [key, value] of uploadedFiles.entries()) {
            console.log(`${key}:`, value);
        }

        // 🔵 3. Submit using fetch
        try {
                const response = await fetch('/Tourism/HandleRequest', {
                method: 'POST',
                body: uploadedFiles
            });

            if (response.ok) {
                    Swal.fire('تم بنجاح', 'تم إرسال البيانات', 'success').then(() => {
                        window.location.href = '/Home/GetAllRequest';  // Redirect to the 'GetAllRequest' page
                    });
            } else {
                Swal.fire('خطأ', 'حدث خطأ في المعالجة', 'error');
            }
        } catch (error) {
            console.error('Upload error:', error);
            Swal.fire('خطأ', 'فشل الإرسال: ' + error.message, 'error');
        }
    }
</script>
}